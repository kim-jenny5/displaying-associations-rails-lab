<h1>Artists & Their Discography</h1>
<% @songs.each do |song| %>
    <ul>
        <li><%= link_to(song_path(song)) do %>
            <%= song.artist_name %> - <%= song.title %>
        <% end %></li>
    </ul>
<% end %>